<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Rendered quote images shared via FileProvider live in cache/shared_images. -->
    <cache-path
        name="shared_images"
        path="shared_images/" />
</paths>
